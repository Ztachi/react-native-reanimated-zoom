{
  "name": "@repo/image-preview",
  "version": "0.1.0",
  "private": false,
  "description": "Reusable zoomable image preview component for React Native / Expo apps. Powered by Reanimated + GestureHandler with pinch zoom, momentum pan, rubber-band and double-tap.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "files": [
    "src"
  ],
  "scripts": {
    "lint": "tsc --noEmit -p tsconfig.json",
    "test": "echo \"No tests yet\"",
    "clean": "echo \"No dist to clean (Metro source package)\""
  },
  "peerDependencies": {
    "react": ">=18.0.0",
    "react-native": ">=0.73.0",
    "react-native-gesture-handler": ">=2.0.0",
    "react-native-reanimated": ">=3.0.0"
  },
  "devDependencies": {
    "@types/react": "~19.1.10",
    "@types/react-native": "^0.73.0"
  },
  "keywords": [
    "react-native",
    "image",
    "preview",
    "zoom",
    "pan",
    "reanimated",
    "gesture-handler"
  ],
  "license": "MIT"
}
